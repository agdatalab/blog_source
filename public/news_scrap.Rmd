---
title: 농업 분야 뉴스 스크랩
author: []
date: '2018-04-13'
slug: news_scrap
categories:
  - 뉴스 스크랩
tags: []

---

#### 농업 현장 소식, 기술동향, 스타트업, 비즈니스 관련 `뉴스`를 모아봅시다.

PC의 파일을 불러와봅시다.
```{r csv read}
dat <- read.csv('cluster.csv')
names(dat)
```


DB에 있는 파일을 불러와 봅시다.
```{r DB connect setup, include=FALSE}
library(dbConnect)
conn <- dbConnect(dbDriver("MySQL"),dbname = 'livestock',host = "147.46.229.85",user = "ais",password = "ezfarm3414" )
      dbGetQuery( conn, "set names 'euckr'" )
      score30 <- dbGetQuery(conn, "SELECT * FROM cut_score")
      dbDisconnect(conn)
```


```{r}
names(score30)
```

<!-- 
list.files("D:/r_data/agdatalab_blog/static/data")
dat <- read.csv('/data/cluster.csv')
nrow(dat) 
-->
